{
  "name": "workbuddy-univer-office",
  "version": "0.1.0",
  "description": "Univer Office: local spreadsheets, documents, slides, Base and Board with one skill and one MCP service. Development preview.",
  "skills": "./skills/",
  "mcpServers": {
    "univer-office": {
      "command": "node",
      "args": ["${CODEBUDDY_PLUGIN_ROOT}/dist/mcp/main.js"],
      "env": { "WORKBUDDY_OFFICE_WORKSPACE": "${CODEBUDDY_PROJECT_DIR}" }
    }
  }
}
